#include <iostream>
#include <array>
#include <string>
#include <iomanip>
#include <cstdio>
#include <vector>
#include <map>
#include <bits/stdc++.h>
using namespace std;

int main() {
    int M;
    int N;
    cin >> M;
    int arr[M];
    for (int i = 0; i < M; i++) {
        cin >> arr[M];
    }
    cin >> N;
    int sumBig = M%N;
}